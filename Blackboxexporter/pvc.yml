apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: expo-pvc
  namespace: prometheus
spec:
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage3